# ceaser cipher
result=''
text=input("Enter the text for ceaser cipher:")
key=int(input('Enter the number of shifts needed:'))
#Encryption
for char in text:
    if char.isalpha():
        if char.isupper():
            result+=chr(ord(char)-ord(a)) 


                            
                
    